[package]
name = "ts-mux"
version.workspace = true
edition.workspace = true
license.workspace = true

# Intentionally zero third-party dependencies: this crate must build and unit-test
# on any host (no Windows / no GPU), so the MPEG-TS byte format can be golden-tested.
[lib]
name = "ts_mux"
